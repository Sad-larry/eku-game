

## 📁 Godot 项目文件夹结构规划（技能构筑动作肉鸽·定制版）

### 一、总体设计原则

| 原则           | 说明                                                         |
| :------------- | :----------------------------------------------------------- |
| **引擎特性**   | 充分利用 Godot 的 `.tscn`（场景）、`.tres`（资源）、`.gd`（脚本）分离特性。 |
| **命名规范**   | 统一使用 `snake_case`（小写 + 下划线），禁止空格和中文命名。 |
| **借鉴与改造** | 以 `structure.txt` 中的成熟结构为**骨架**，以 `项目解析.txt` 中的**肉鸽模块建议**为**血肉**进行定制。 |
| **按需裁剪**   | 本项目暂无局外永久养成、多角色系统，初期删除 `progression/`、`players/` 多角色目录，后期可再加回。 |

------

### 二、根目录结构总览

text

```text
eku-game/                         # 项目根目录
├── .godot/                       # 引擎自动生成，忽略
├── addons/                       # 第三方插件（如用到的 GdUnit、Deeplink 等）
├── assets/                       # 原始素材（未导入、未加工的美术/音频源文件）
├── autoloads/                    # 全局单例脚本（AutoLoad）
├── effects/                      # 着色器材质与视觉特效资源
├── prefabs/                      # 可复用的预制场景（PackedScene）
├── resources/                    # 数据资源文件（.tres）
├── scenes/                       # 游戏主场景（各界面入口与核心流程）
├── scripts/                      # 纯脚本文件（不直接挂载的类、系统、工具）
├── shaders/                      # 纯着色器代码（.gdshader）
├── styles/                       # UI 主题样式（Theme .tres）
├── project.godot                 # 项目配置
└── README.md                     # 项目协作说明
```



------

### 三、各目录详细规划与定制说明

> **标注说明：**
>
> - `✅ 已存在`：参考 `structure.txt` 中已有且本项目直接沿用的模块。
> - `🆕 新增/定制`：根据本项目特性需要创建或修改的模块。
> - `⏸️ 暂缓`：初期 MVP 不涉及，但预留目录结构。

------

#### 3.1 `autoloads/` —— 全局管理器

text

```
autoloads/
├── global.gd                     ✅ 全局信号、枚举、快捷函数
├── game_manager.gd               🆕 游戏状态（主菜单/战斗中/暂停）
├── audio_manager.gd              ✅ 音效/音乐管理
├── save_manager.gd               ✅ 基础存档（至少记录最高连击数）
├── input_manager.gd              🆕 处理按键映射与输入缓冲
└── skill_library.gd              🆕 全局技能池管理（读取所有技能资源）
```



**定制说明：**

- `skill_library.gd` 是本项目的核心全局单例，负责从 `resources/skills/` 加载所有技能数据，供构筑界面调用。

------

#### 3.2 `prefabs/` —— 预制场景（实体与对象）

text

```
prefabs/
├── entities/
│   ├── player/                   🆕 玩家角色（目前只有一个）
│   │   └── player.tscn
│   └── enemies/                  🆕 敌人基类与变体
│       ├── enemy_base.tscn
│       ├── enemy_melee.tscn
│       ├── enemy_ranged.tscn
│       └── enemy_elite.tscn
├── objects/
│   ├── pickups/                  ✅ 拾取物（经验球、血包）
│   │   ├── pickup_base.tscn
│   │   ├── pickup_health.tscn
│   │   └── pickup_energy.tscn
│   ├── projectiles/              ✅ 子弹/弹幕
│   │   ├── projectile_base.tscn
│   │   ├── projectile_sword_beam.tscn
│   │   └── projectile_enemy_bullet.tscn
│   └── interactables/            🆕 可交互物（宝箱、祭坛）
│       └── chest.tscn
├── environment/
│   ├── doors/                    🆕 房间门（清场后打开）
│   │   └── door.tscn
│   ├── traps/                    🆕 地图陷阱
│   │   └── spike_trap.tscn
│   └── breakables/               🆕 可破坏物（木桶）
│       └── barrel.tscn
└── fx/                           ✅ 特效
    ├── hit_effect.tscn           # 受击特效
    ├── flash_effect.tscn         # 闪现残影
    └── skill_cast.tscn           # 技能释放特效
```



**定制说明：**

- 本项目强调 **「闪现连击」** 的视觉反馈，`fx/flash_effect.tscn` 需要单独制作并配合 `effects/` 中的着色器。

------

#### 3.3 `resources/` —— 数据驱动核心

text

```
resources/
├── data/
│   ├── skills/                   🆕 技能数据（替代传统武器）
│   │   ├── skill_base.gd         # 技能资源基类脚本
│   │   ├── initiator/            # 起手技
│   │   │   ├── skill_mark.tres
│   │   │   └── skill_berserk.tres
│   │   ├── finisher/             # 连携技
│   │   │   ├── skill_flash_chain.tres   # 闪现连击
│   │   │   └── skill_uppercut.tres
│   │   ├── control/              # 场控技
│   │   │   └── skill_vortex.tres
│   │   └── survival/             # 生存技
│   │       └── skill_phantom_dash.tres
│   ├── enemies/                  ✅ 敌人属性
│   │   ├── stats_enemy_base.tres
│   │   ├── stats_enemy_melee.tres
│   │   └── stats_enemy_ranged.tres
│   ├── player/                   🆕 玩家基础属性
│   │   └── stats_player.tres
│   ├── waves/                    ✅ 敌人波次配置
│   │   └── wave_data_floor_1.tres
│   ├── rooms/                    🆕 房间配置
│   │   ├── room_reward.tres
│   │   └── room_shop.tres
│   └── upgrades/                 ⏸️ 暂缓（局内临时强化用，局外永久不用）
├── configs/
│   ├── game_config.tres          🆕 屏幕尺寸、像素缩放设置
│   ├── balance_config.tres       🆕 伤害公式系数、连击时间窗口
│   └── input_map_config.tres     🆕 键位映射配置
└── themes/                       ✅ UI 主题样式
    ├── default_theme.tres
    └── pixel_font.tres
```



**定制说明（重点）：**

- **技能资源分类** 直接对应您上一轮确定的 **「起手技/连携技/场控技/生存技」** 分类体系。
- 每个 `.tres` 技能资源包含：技能名称、图标路径、冷却时间、伤害倍率、**机制类别枚举值**、关联特效路径。

------

#### 3.4 `scenes/` —— 游戏流程场景

text

```
scenes/
├── main/                         # 主菜单与外围系统
│   ├── main_menu.tscn            ✅
│   ├── skill_select.tscn         🆕 战前技能构筑界面
│   └── settings.tscn             ✅
├── game/                         # 核心游戏场景
│   ├── game_world.tscn           🆕 游戏世界根节点（管理房间切换）
│   ├── room_base.tscn            🆕 房间模板基类
│   ├── room_battle.tscn          🆕 战斗房间实例
│   ├── room_boss.tscn            🆕 BOSS 房间实例
│   └── room_shop.tscn            🆕 商店房间实例
├── ui/                           # 全局 HUD 与弹窗
│   ├── hud.tscn                  🆕 战斗中显示：血条、技能栏、连击数
│   ├── pause_menu.tscn           ✅
│   ├── game_over.tscn            ✅
│   └── floating_text.tscn        ✅ 伤害/治疗跳字
└── transitions/                  🆕 过场动画（开门、传送）
    └── door_transition.tscn
```



**定制说明：**

- `skill_select.tscn` 是本项目的**独有界面**，用于展示技能池并让玩家勾选 4-6 个携带技能。
- `hud.tscn` 必须醒目显示**当前连击数**和**技能冷却状态**。

------

#### 3.5 `scripts/` —— 系统与逻辑脚本

text

```
scripts/
├── systems/
│   ├── combat/                   ✅ 战斗系统
│   │   ├── damage_calculator.gd
│   │   └── combo_manager.gd      🆕 连击计时与奖励计算
│   ├── skills/                   🆕 技能系统（核心）
│   │   ├── skill_container.gd    # 玩家携带技能容器
│   │   ├── skill_cooldown.gd     # 冷却管理
│   │   └── skill_activator.gd    # 技能释放逻辑（闪现连击判定）
│   ├── room_generation/          🆕 房间生成
│   │   ├── room_generator.gd
│   │   └── room_layouts.gd       # 预设房间布局数据
│   ├── loot/                     🆕 掉落系统
│   │   └── loot_table.gd
│   ├── inventory/                ⏸️ 暂缓（无复杂背包）
│   └── events/                   🆕 事件系统（触发宝箱、NPC对话）
│       └── game_event_bus.gd
├── components/                   ✅ 组件
│   ├── health_component.gd
│   ├── hurtbox_component.gd
│   ├── hitbox_component.gd
│   └── state_machine_component.gd 🆕 敌人 AI 状态机
├── behaviors/                    ✅ AI 行为
│   ├── enemy_ai/
│   │   ├── chase_behavior.gd
│   │   ├── attack_behavior.gd
│   │   └── patrol_behavior.gd
│   └── projectile_behavior.gd    # 子弹移动逻辑
└── utils/                        ✅ 工具
    ├── helper.gd
    ├── math_util.gd
    └── scene_loader.gd
```



**定制说明：**

- `combo_manager.gd` 专门管理闪现连击的**时间窗口判定**和**连击数衰减**。
- `skill_activator.gd` 中实现闪现连击的核心逻辑：命中时检测附近敌人 → 计算闪现位置 → 播放位移动画 → 造成伤害。

------

#### 3.6 `effects/` 与 `shaders/` —— 视觉魔法层

text

```
effects/
├── shaders/                      # 材质文件（.tres）
│   ├── flash_material.tres       ✅ 受击闪白
│   ├── dissolve_material.tres    # 敌人死亡消散
│   └── outline_material.tres     # 选中/高亮轮廓
└── particles/                    🆕 粒子系统场景（作为预制体放入 prefabs/fx/）

shaders/                          # 纯着色器代码
├── flash.gdshader                ✅
├── dissolve.gdshader
├── outline.gdshader              ✅
└── pixel_snap.gdshader           🆕 保持像素旋转不模糊（针对 2.5D 旋转）
```



**定制说明：**

- `pixel_snap.gdshader` 用于确保像素风格的角色在旋转、缩放时保持硬边缘，避免产生模糊的混合像素。

------

### 四、与参考文件的差异对照表

| 模块         | `structure.txt` 原有                 | 本项目定制化调整                                             | 原因                                       |
| :----------- | :----------------------------------- | :----------------------------------------------------------- | :----------------------------------------- |
| **武器系统** | 近战/远程武器分离                    | 删除 `weapons/` 目录，改为 `skills/`                         | 本作以**技能**为核心战斗手段，而非武器拾取 |
| **玩家角色** | 多个玩家变体（Brawler, Bunny等）     | 初期仅保留一个 `player.tscn`                                 | 专注核心玩法验证，后期再扩展角色           |
| **局外成长** | 无明确模块                           | 明确标注 `⏸️ 暂缓`                                            | MVP 先验证局内构筑，防止系统膨胀           |
| **技能构筑** | 无                                   | 新增 `skill_select.tscn`、`skill_library.gd`、`resources/data/skills/` | **本作核心差异点**                         |
| **连击系统** | 无                                   | 新增 `combo_manager.gd`                                      | 对应闪现连击的玩法承诺                     |
| **房间生成** | 无（`structure.txt` 是竞技场生存类） | 新增 `room_generation/` 与房间场景                           | 改为**房间制探索**，更符合传统肉鸽节奏     |

------



------

### 五、初期 MVP 开发建议

为了避免陷入“搭建完美框架但一行代码没写”的陷阱，请按以下顺序逐步填充文件夹：

1. **第一周**：只填充 `scenes/main/`、`prefabs/entities/player/`、`prefabs/entities/enemies/`（做 1 个敌人），完成**灰盒移动与基础攻击**。
2. **第二周**：填充 `resources/data/skills/` 和 `scripts/skills/`，实现**第一个技能（闪现连击）** 的逻辑原型。
3. **第三周**：填充 `scenes/game/room_base.tscn` 和 `scripts/systems/room_generation/`，完成**房间生成与切换**。
4. **第四周**：填充 `scenes/ui/hud.tscn` 和 `scripts/systems/combat/combo_manager.gd`，打磨**连击显示与打击感**。

> **原则：哪个文件夹开始放东西，就证明哪个系统开始进入开发。空文件夹不产生任何维护成本，但提供了明确的导航。**

------

